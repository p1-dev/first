질문:
- 내장 구조체의 선언 순서는?
- 구조체 멤버 접근의 내부 로직은?

- malloc(sizeof(int*)))가 아니라 sizeof(int)인 이유 stride

- Person p1 = { 25, "Alice" }; 이 문법과 문자열 리터럴 문법 비교하기, 리터럴의 내부 로직은?


- 구조체 안의 int는 포인터가 아닌채 선언될 수 있다. 근데 구조체 안에 내장 구조체를 선언하면 
포인터아닐때:하위 구조체보다 상위 구조체가 무조건 위에서 선언됐어야함
포인터일때:하위 구조체와 상위 구조체 순서 상관없음.

int의 경우 선언하는 순간 4바이트만 주소에서 할당하면된다.
그러나 구조체의 경우 바이트 수가 딱 정해져있지 않다. 구조체의 바이트 수는 모든 멤버들의 바이트 수의 합과 동일하다.
근데 구조체 안에 상위 구조체를 넣었다면 이 상위 구조체의 크기가 이미 선언이 되있어야 그 크기를 알 수 있고 offset을 컴파일 타임 때 결정가능함.
구조체 안에 구조체 포인터를 넣는 경우는 상황이 다르다. 어떤 주소라도 포인터를 쓰면 8바이트만 차지하므로 뒷일 생각없이 일단 8byte의 offset만 
컴파일 타임 때 확보하면 된다.


typedef struct circle{
  char label
  int size
  char name[3]
} circle;

int main()
{
  circle x;
  x.width=1;
  x.size=2;
}
멤버 연산자의 내부 로직은?






❗ 구조체 멤버 중 char* name은 패딩되지 않습니다.
왜냐하면 char*는 포인터 타입이고, 포인터는 4바이트(32비트 시스템) 또는 **8바이트(64비트 시스템)**를 차지하는 고정 크기의 타입입니다. char*이기 때문에 이름 문자열이 들어가는 공간이 아니라, **문자열이 저장된 위치(주소)**만 저장하는 것입니다.



- malloc(sizeof(int*)))가 아니라 sizeof(int)인 이유 stride
메모리의 상한선을 정하는 것이다.
    s1.name = "Bob";
    s1.scores = (int*)malloc(sizeof(int) * 3);





struct Example {
    char a;      // 1 byte
    int b;       // 4 bytes
};
char a는 1바이트지만, int b가 4바이트 정렬을 요구하므로,
컴파일러가 a 뒤에 3바이트 패딩을 삽입해 b가 4바이트 정렬 위치에서 시작되게 만듭니다.






리터럴 안되는 거 ㅡ 각 인덱스마다 넣어줘야함
struct Student {
    char name[20];
    int scores[5];
}; 이거 메인에서 score에 리터럴로 값 넣는거 못하나
ㅡ>
struct Student s1;
s1.scores = {90, 85, 80, 95, 100};  // ❌ 컴파일 오류

대신 memcpy나 memset 가능하다는데 내부 로직구현하기
    int temp_scores[5] = {90, 85, 80, 95, 100};
    memcpy(s1.scores, temp_scores, sizeof(temp_scores));  // 배열 통째 복사

    for (int i = 0; i < 5; i++) {
        printf("점수 %d: %d\n", i + 1, s1.scores[i]);
    }






패팅과 바인딩 인덱스 
만약 바인딩 인덱스를 통해서 접근한다면
모든 변수들은 list[0],[1],[2],.. 이런식일테고,
이때 [2]를 접근하고 싶다면 가장 쉽게 하는 방법은,
sizeof(list[0])*2 에서 읽어오면 가장 베스트일 것이다.
그런데 만약 [0]과 [2]의 size가 다르다면 위의 논리는 성립불가다.
더 자세히 말하자면, 모든 원소의 사이즈가 같아야 저게 가능함.

근데 단, char a[3]을 생각해보자. 얘는 char가 3개가 붙어있으니,
만약 int 멤버랑 같이 있다면 4에 맞춰, 각각 4바이트씩 할당하여 4,4,4=12 할당해야하나?
아니다. 왜냐하면 char a[3]은 첫 번째 원소의 주소만 갖고 있으면 되니까. 만약 a가 int 크기 이하라면 그냥 4바이트 하나만으로 충분하고,
5라면, 패팅으로 3 더해주면 되겠다.



음.. 좀 비논리적이지만, 그.. 내부범위에서 충분히 접근가능하다 하면 패딩을 안쓰나?
하지만 그렇게치면 index*sizeof()로서 접근이 불가능할것같은데;
typedef struct circle_optimized {
    int size;        // 4B
    char label;      // 1B
    char name[3];    // 3B
} circle_optimized;






혹시 구조체 하나 만들고 그 뒤에 바로 또 구조체 만들면 바로 연속적으로 붙어서 만들어지나? 그래서 패딩을 마지막까지 해주려는건가?



즉, 비유적으로 표현하면 a는 자기 다음 친구인 b의 눈치를 봐야 한다는 것이다.
->
오류: 맨 마지막 멤버의 경우에는 그 뒤에 올 애도 없는데 패딩을 왜 함?


단순히 하나의 논리로 되는게 아니라 투트랙으로 하는거다;
1. 맨 마지막 각 멤버를 제외한 앞 멤버들은 그 바로 직후의 멤버의 alignment에 맞추며 그 alignment는 구조체에서 가장 큰
멤버의 alignment가 아니라 본인의 alignment 말하는거임;;  
2. 맨 마지막 멤버의 패팅: 구조체 → 내부 멤버들 중 가장 큰 alignment를 기준으로 함
+
0.첫 주소도 그 멤버의 alignment에 딱 맞춰서 가야하는것인가 운영체제?


구조체 에 이름 안써도 대부분 괜찮지만 자기 참조 하는 노드 일 때 오류 난다는 그거 뭐였더라
typedef struct
{
    int a;
    short b;
    char c;
    
    /* int - 4, short - 2, chaar - 1
       모두 합해서 7바이트! */
} Sample;



https://dafher-diary.tistory.com/12
   printf("%d\n", (char*)&f.d - (char*)&f.s.b);
근데 char*는 1칸이 아니라 8바이트 아니냐?
->
(char*)&f.d - (char*)&f
= 140732754922440 - 140732754922416
= 24 

char* p1 =(char*)&f.d ;
char* p2 = (char*)&f;

ptrdiff_t diff = p2 - p1;
->
이거 이유는, 
그 문서
및
이거 그 +n 연산자로까지 이어짐

